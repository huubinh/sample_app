<li>
  <%= gravatar_for user, size: Settings.user.index_avatar_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".delete_user"), user, method: :delete, data: { confirm: t(".delete_confirm") } %>
  <% end %>
</li>
